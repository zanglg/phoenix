[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
rust-version = "1.95"
publish = false

[dependencies]
phoenix-kernel = { path = "../kernel" }

[workspace]
resolver = "3"

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
unused_must_use = "deny"

[lints.clippy]
correctness = "deny"
suspicious = "deny"
